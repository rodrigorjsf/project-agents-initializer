<!-- TEMPLATE: Scoped CLAUDE.md (one per detected scope). Target: 10-30 lines.
     Only info that DIFFERS from root CLAUDE.md; one scope per file.
     Placement: [scope-path]/CLAUDE.md. Auto-loaded by Claude Code when working in this subdirectory.
-->

# [One-sentence scope description]

## Tooling

<!-- CONDITIONAL on commands that differ from root. Omit if scope uses same tooling. -->
- Build: `[scope-specific command]`
- Test: `[scope-specific command]`
<!-- CONDITIONAL on migration commands not in root. -->
- Migrate: `[scope-specific migration command]`

## Conventions

<!-- Only non-obvious, scope-specific conventions. Every instruction specific and verifiable. -->
- [Specific, verifiable instruction]
